

def sum_as_string(one: int, two: int) -> str:
    """Sum two integers and return the sum as a string.

    Example:
        >>> from string_sum import sum_as_string
        >>> sum_as_string(1, 2)
        '3'
    """
    ...
